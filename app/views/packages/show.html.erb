<% @title = "The " + @package.name + " package" %>

<h1><%= h @package.name %></h1>
<% if @version.url -%>
  <p><%= @version.urls.map{|url| link_to url, url}.join(", ") %></p>
<% end -%>

<p><%= h @version.title %></p>
<p><%= h @version.description %></p>

<h2><%=h @package.name %> has <%= pluralize @package.versions.length, "version" %>:</h2>

<ul>
<% for version in @package.versions -%>
  <li><%= link_to version.vname, [@package, version] %></li>
<% end -%>
</ul>