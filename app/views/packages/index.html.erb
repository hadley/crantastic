<% @title = "#{Package.count} R packages"; @plural = true %>
<% @atom = packages_path(:format => :atom) %>

<h1>R Packages</h1>

<div class="content">
  <p>There are <%= Package.count %> packages listed on crantastic.</p>
  <p>Want to view all packages in one page? <%= link_to "Click here", all_packages_url -%></p>

  <p>
    <span class="float-l" style="margin-right: 5px;">Search Package by
    Name:</span>
  </p>

  <div class="float-l">
    <form action="/packages" method="get">
      <div><input id="package-search" name="search" type="text" /></div>
    </form>

    <%= image_tag("spinner.gif",
                  :id => "spinner",
                  :class => 'float-l',
                  :style => "margin-left: 5px; display: none;") -%>
  </div>
  <%= clear_div %>
  <br />

     <p>You can use the tilde character (~) at the end of your query to activate
     fuzzy matching.</p>

  <div id="packages_list">
    <%= render :partial => "packages/list" -%>
  </div>

</div>
